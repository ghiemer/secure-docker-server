# Lösche alte Regeln
-D

# Buffer Size erhöhen
-b 8192

# Failure Mode (1=log, 2=panic)
-f 1

# -- Überwache kritische Dateien --

# Passwörter und Gruppen
-w /etc/group -p wa -k identity
-w /etc/passwd -p wa -k identity
-w /etc/gshadow -p wa -k identity
-w /etc/shadow -p wa -k identity

# SSH Konfiguration
-w /etc/ssh/sshd_config -p wa -k ssh_config

# Docker Daemon Config
-w /etc/docker/daemon.json -p wa -k docker_config

# Systemd Units (Services verändern)
-w /etc/systemd/ -p wa -k systemd

# -- Netzwerk --
-w /etc/hosts -p wa -k system_locale
-w /etc/network/ -p wa -k system_locale

# -- Dateirechte Änderungen (chmod/chown) --
-a always,exit -F arch=b64 -S chmod -S fchmod -S fchmodat -F auid>=1000 -F auid!=-1 -k perm_mod
-a always,exit -F arch=b64 -S chown -S fchown -S fchownat -S lchown -F auid>=1000 -F auid!=-1 -k perm_mod

# Lock the rules configuration
-e 2
